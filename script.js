function setup() {
  createCanvas(windowWidth-20,windowHeight-20);
  background(100);
  setupKeyBindings();
  generateNewPoints();
  rectMode(CORNERS);
  ellipseMode(RADIUS);
  noLoop();
  noFill();
}

function draw() {

}